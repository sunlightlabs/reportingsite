import csv, sqlite3


conn = sqlite3.connect('reporting.sql')

indexReader = csv.reader(open('import-tags.csv', 'r'))
for row in indexReader:
    q = "INSERT INTO tagging_taggeditem (object_id, tag_id) VALUES ("+str(row[0])+", "+str(row[1])+")"
    conn.execute(q)
conn.commit()
